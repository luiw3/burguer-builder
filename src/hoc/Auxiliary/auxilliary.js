
// Funciona como um componente sem nenhuma lógica ou elementos JSX, apenas como um wrapper dinâmico
// que pode ser utilizado em vários componentes ao invés de utilizar uma div

const aux = (props)=> props.children;

export default aux;